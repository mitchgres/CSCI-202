Selection Sort
----------------------------------------
The algorithm will find the smallest valued
element in the list, and will then move that
smallest element to the bottom of the list. 
The algorithm's growth function is linear. 

I(n) = 3n-3