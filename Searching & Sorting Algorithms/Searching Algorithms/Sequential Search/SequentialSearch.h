#include "../LinkedList.h"

template <class Type>

class SequentialSearch {
    public:
        int searchArray(const Type[], int, const Type&); 
        int searchList(const LinkedList<Type>, const Type&);      
};