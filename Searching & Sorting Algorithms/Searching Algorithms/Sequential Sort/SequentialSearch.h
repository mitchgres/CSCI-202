#include <vector>

template <class Type>

class SequentialSearch {
    public:
        int searchArray(const Type[], int, const Type&); 
        int searchList(const vector<Type>, const Type&);      
};